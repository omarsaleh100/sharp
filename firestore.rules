rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // This rule allows all reads and writes for development.
    // We will make this more secure before going live.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}